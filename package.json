{
  "name": "i0c-redirect-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsup --config tsup.config.ts",
    "dev:cf": "wrangler dev",
    "deploy:cf": "wrangler deploy",
    "dev:dn": "deno run --allow-net --allow-env --import-map=deno/import_map.json deno/main.ts",
    "deploy:dn": "deno deploy --import-map=deno/import_map.json deno/main.ts",
    "build:vc": "npm run build && node scripts/prepare-vercel-output.mjs",
    "dev:vc": "npm run build && vercel dev",
    "deploy:vc": "vercel --prod"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251213.0",
    "@esbuild-plugins/tsconfig-paths": "^0.1.2",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3",
    "vercel": "^50.0.1",
    "wrangler": "^4.54.0"
  }
}
