{
  "name": "i0c-redirect-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "dev:cf": "wrangler dev",
    "deploy:cf": "wrangler deploy",
    "dev:vc": "npm run build && vercel dev", 
    "deploy:vc": "vercel --prod"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251213.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.54.0",
    "vercel": "latest" 
  }
}
