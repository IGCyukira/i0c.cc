{
  "name": "i0c-redirect-worker",
  "version": "0.1.0",
  "private": true,
  "overrides": {
    "@vercel/fun": {
      "tar": "7.5.7"
    },
    "@vercel/node": {
      "undici": "5.29.0"
    },
    "tar": "7.5.7",
    "qs": "6.14.1"
  },
  "scripts": {
    "build": "tsup --config tsup.config.ts && node scripts/post-build.mjs",
    "dev:cf": "wrangler dev",
    "deploy:cf": "wrangler deploy",
    "build:vc": "npm run build",
    "build:vc:watch": "tsup --config tsup.config.ts --watch --onSuccess \"node scripts/post-build.mjs\"",
    "dev:vc": "concurrently \"npm run build:vc:watch\" \"vercel dev\"",
    "deploy:vc": "vercel --prod",
    "build:nf": "npm run build",
    "dev:nf": "npx --yes netlify-cli@latest dev",
    "deploy:nf": "npx --yes netlify-cli@latest deploy --build"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251213.0",
    "@esbuild-plugins/tsconfig-paths": "^0.1.2",
    "concurrently": "^9.2.1",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3",
    "vercel": "^50.9.5",
    "wrangler": "^4.61.1"
  }
}
